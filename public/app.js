const $=id=>document.getElementById(id);let token=localStorage.token,user=JSON.parse(localStorage.user||"null"),ws,room=null,role="viewer",screenStream=null,micStream=null,micMuted=false,deafened=false,screenReady=false;const voicePeers=new Map(),screenPeers=new Map(),iceQueue=new Map(),pendingScreenViewers=new Set();
async function api(p,b){const r=await fetch(p,{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify(b)}),d=await r.json();if(!r.ok)throw Error(d.error);return d}
function boot(){if(!token||!user)return;$("auth").classList.add("hidden");$("app").classList.remove("hidden");$("profileName").textContent=user.username;connect()}
$("login").onclick=()=>auth("/api/login");$("register").onclick=()=>auth("/api/register");async function auth(p){try{const d=await api(p,{username:$("user").value,password:$("pass").value});token=d.token;user=d.user;localStorage.token=token;localStorage.user=JSON.stringify(user);boot()}catch(e){$("authMsg").textContent=e.message}}
function connect(){const p=location.protocol==="https:"?"wss":"ws";ws=new WebSocket(`${p}://${location.host}`);ws.onopen=()=>{send({type:"auth",token});send({type:"list-rooms"})};ws.onmessage=e=>handle(JSON.parse(e.data))}
function send(x){if(ws?.readyState===1)ws.send(JSON.stringify(x))}function esc(s){return String(s||"").replace(/[&<>"]/g,c=>({"&":"&amp;","<":"&lt;",">":"&gt;",'"':"&quot;"}[c]))}
function renderRooms(rs){$("roomList").innerHTML=rs.map(r=>`<button class="roomItem" data-id="${r.id}">${r.private?"🔒":"#"} ${esc(r.name)}</button>`).join("");document.querySelectorAll(".roomItem").forEach(b=>b.onclick=()=>join(b.dataset.id))}
$("newRoom").onclick=()=>$("modal").classList.remove("hidden");$("closeModal").onclick=()=>$("modal").classList.add("hidden");$("private").onchange=e=>$("roomPassword").classList.toggle("hidden",!e.target.checked);$("createRoom").onclick=()=>send({type:"create-room",name:$("newRoomName").value,private:$("private").checked,password:$("roomPassword").value});
function join(id){const pw=prompt("Código da sala (vazio se pública):");if(pw===null)return;role="viewer";send({type:"join",roomId:id,password:pw,role})}
$("broadcast").onclick=async()=>{if(!room)return alert("Entre em uma sala primeiro.");if(room.broadcaster)return alert("Já existe uma transmissão.");role="broadcaster";send({type:"join",roomId:room.id,password:"",role})};
$("invite").onclick=async()=>{if(!room)return;await navigator.clipboard.writeText(location.origin+location.pathname+"?room="+room.id);alert("Convite copiado!")};
$("mic").onclick=()=>{if(!micStream)return;micMuted=!micMuted;micStream.getAudioTracks().forEach(t=>t.enabled=!micMuted);$("mic").textContent=micMuted?"🔇 Microfone":"🎤 Microfone"};
$("deafen").onclick=()=>{deafened=!deafened;document.querySelectorAll("audio.voice").forEach(a=>a.muted=deafened);$("deafen").textContent=deafened?"🔇 Surdo":"🎧 Surdo"};
$("stop").onclick=stopScreen;$("send").onclick=chat;$("message").onkeydown=e=>e.key==="Enter"&&chat();function chat(){const t=$("message").value.trim();if(t&&room){send({type:"chat",text:t});$("message").value=""}}
async function getMic(){if(micStream)return micStream;try{return micStream=await navigator.mediaDevices.getUserMedia({audio:{echoCancellation:true,noiseSuppression:true,autoGainControl:true}})}catch{return null}}
function cfg(){return{iceServers:[{urls:"stun:stun.l.google.com:19302"}]}}
async function startScreen(){try{const h=+$("quality").value,f=+$("fps").value;screenStream=await navigator.mediaDevices.getDisplayMedia({video:{width:{ideal:h*16/9},height:{ideal:h},frameRate:{ideal:f,max:f}},audio:true});$("video").srcObject=screenStream;$("empty").classList.add("hidden");$("liveBadge").classList.remove("hidden");$("stop").classList.remove("hidden");screenStream.getVideoTracks()[0].onended=stopScreen;await getMic();screenReady=true;send({type:"screen-ready"});for(const id of pendingScreenViewers){pendingScreenViewers.delete(id);await makeScreenPeer(id)}}catch{}}
async function makeScreenPeer(id){if(!screenStream){pendingScreenViewers.add(id);return}if(screenPeers.has(id))return;const pc=new RTCPeerConnection(cfg());screenPeers.set(id,pc);screenStream.getTracks().forEach(t=>pc.addTrack(t,screenStream));pc.onicecandidate=e=>e.candidate&&send({type:"candidate",channel:"screen",target:id,candidate:e.candidate});pc.onconnectionstatechange=()=>{if(["failed","closed","disconnected"].includes(pc.connectionState)){pc.close();screenPeers.delete(id)}};const o=await pc.createOffer();await pc.setLocalDescription(o);send({type:"offer",channel:"screen",target:id,offer:o})}
function stopScreen(){screenReady=false;pendingScreenViewers.clear();screenStream?.getTracks().forEach(t=>t.stop());screenStream=null;screenPeers.forEach(p=>p.close());screenPeers.clear();$("video").srcObject=null;$("empty").classList.remove("hidden");$("liveBadge").classList.add("hidden");$("stop").classList.add("hidden");send({type:"stream-stopped"})}
async function joinVoice(id,initiator=true){if(voicePeers.has(id)||!await getMic())return;const pc=new RTCPeerConnection(cfg());voicePeers.set(id,pc);micStream.getTracks().forEach(t=>pc.addTrack(t,micStream));pc.onicecandidate=e=>e.candidate&&send({type:"candidate",channel:"voice",target:id,candidate:e.candidate});pc.onconnectionstatechange=()=>{if(["failed","closed","disconnected"].includes(pc.connectionState)){pc.close();voicePeers.delete(id);document.querySelector(`audio[data-peer="${id}"]`)?.remove()}};pc.ontrack=e=>{let a=document.querySelector(`audio[data-peer="${id}"]`);if(!a){a=document.createElement("audio");a.className="voice";a.dataset.peer=id;a.autoplay=true;document.body.appendChild(a)}a.srcObject=e.streams[0];a.muted=deafened};if(initiator){const o=await pc.createOffer();await pc.setLocalDescription(o);send({type:"offer",channel:"voice",target:id,offer:o})}}
async function handle(m){if(m.type==="error")return alert(m.message);if(m.type==="rooms")return renderRooms(m.rooms);if(m.type==="room-created"){join(m.room.id);$("modal").classList.add("hidden");return}if(m.type==="joined"){room=m.room;$("title").textContent=room.name;$("subtitle").textContent=room.private?"🔒 Sala privada":"🌐 Sala pública";$("welcome").classList.add("hidden");$("roomView").classList.remove("hidden");updateMembers(m.members);if(role==="broadcaster")await startScreen();else await getMic();send({type:"voice-join"});return}if(m.type==="members")return updateMembers(m.members);if(m.type==="chat"){ $("messages").insertAdjacentHTML("beforeend",`<div class="msg"><b>${esc(m.user)}</b>: ${esc(m.text)}</div>`);return}if(m.type==="viewer-joined"&&role==="broadcaster"){if(screenReady)return makeScreenPeer(m.viewerId);pendingScreenViewers.add(m.viewerId);return}if(m.type==="voice-peer")return joinVoice(m.peerId,m.initiator);if(m.type==="viewer-left"){voicePeers.get(m.viewerId)?.close();voicePeers.delete(m.viewerId);screenPeers.get(m.viewerId)?.close();screenPeers.delete(m.viewerId);document.querySelector(`audio[data-peer="${m.viewerId}"]`)?.remove();return}if(m.type==="stream-stopped"&&role!=="broadcaster"){$("video").srcObject=null;$("empty").classList.remove("hidden");$("liveBadge").classList.add("hidden");return}
if(m.type==="offer"&&m.channel==="screen"){let pc=screenPeers.get(m.from);if(!pc){pc=new RTCPeerConnection(cfg());screenPeers.set(m.from,pc);pc.ontrack=e=>{$("video").srcObject=e.streams[0];$("empty").classList.add("hidden");$("liveBadge").classList.remove("hidden")};pc.onicecandidate=e=>e.candidate&&send({type:"candidate",channel:"screen",target:m.from,candidate:e.candidate})}await pc.setRemoteDescription(m.offer);for(const c of iceQueue.get(m.from)||[])await pc.addIceCandidate(c);iceQueue.delete(m.from);const ans=await pc.createAnswer();await pc.setLocalDescription(ans);send({type:"answer",channel:"screen",target:m.from,answer:ans});return}
if(m.type==="offer"&&m.channel==="voice"){if(!await getMic())return;let pc=voicePeers.get(m.from);if(!pc){pc=new RTCPeerConnection(cfg());voicePeers.set(m.from,pc);micStream.getTracks().forEach(t=>pc.addTrack(t,micStream));pc.ontrack=e=>{let a=document.querySelector(`audio[data-peer="${m.from}"]`);if(!a){a=document.createElement("audio");a.className="voice";a.dataset.peer=m.from;a.autoplay=true;document.body.appendChild(a)}a.srcObject=e.streams[0];a.muted=deafened};pc.onicecandidate=e=>e.candidate&&send({type:"candidate",channel:"voice",target:m.from,candidate:e.candidate})}await pc.setRemoteDescription(m.offer);for(const c of iceQueue.get(m.from)||[])await pc.addIceCandidate(c);iceQueue.delete(m.from);const ans=await pc.createAnswer();await pc.setLocalDescription(ans);send({type:"answer",channel:"voice",target:m.from,answer:ans});return}
if(m.type==="answer"){const map=m.channel==="voice"?voicePeers:screenPeers,pc=map.get(m.from);if(pc){await pc.setRemoteDescription(m.answer);for(const c of iceQueue.get(m.from)||[])await pc.addIceCandidate(c);iceQueue.delete(m.from)}return}if(m.type==="candidate"){const map=m.channel==="voice"?voicePeers:screenPeers,pc=map.get(m.from);if(pc?.remoteDescription)await pc.addIceCandidate(m.candidate);else{if(!iceQueue.has(m.from))iceQueue.set(m.from,[]);iceQueue.get(m.from).push(m.candidate)}}}
function updateMembers(ms){$("memberCount").textContent=ms.length;$("members").innerHTML=ms.map(x=>`<div class="member"><span class="dot">●</span>${esc(x.username)} ${x.role==="broadcaster"?"📺":""}</div>`).join("")}
$("logout").onclick=()=>{localStorage.clear();location.reload()};boot();const q=new URLSearchParams(location.search);if(q.get("room"))setTimeout(()=>join(q.get("room")),1200);